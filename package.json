{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "ignite-call",
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build":"turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.1"
  },
  "packageManager": "npm@10.7.0"
}
